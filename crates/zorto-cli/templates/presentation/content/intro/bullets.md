+++
title = "What you can do"
weight = 20
+++

## What you can do

- Write each slide as its own markdown file
- Order slides with the `weight` field in frontmatter
- Style with built-in `[extra]` knobs: layout, backgrounds, transitions
- Mix in shortcodes for fragments, columns, speaker notes
- Run Python at build time and bake the output into the deck
